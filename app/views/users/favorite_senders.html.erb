<% content_for :title do %>
  <%= "受信箱(お気に入りユーザー) | あ" %>
<% end %>

<%= render "home/adsense" %>

<% if current_user.receive_range == "all" && current_user.likings.count > 0 %><%# 受け取り範囲がユーザー全員の@userはお気に入りユーザーだけに絞り込んでメッセージを見られるようにする %>
  <div id="senders_btn"><%= link_to "すべてのメッセージを確認", senders_path %></div>
<% end %>

<div id="users_ajax">
  <%= render "users", title:@title, users:@users %>
</div>

<%= render "home/adsense" %>

